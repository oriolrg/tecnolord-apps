# LOCAL ENVIRONMENT — SYNTHETIC ONLY
# Copy this file to local.env and adjust if needed.
# Never commit local.env.

METEOLORD_ENV=local
METEOLORD_PROVIDER_MODE=synthetic
METEOLORD_ALLOW_SYNTHETIC=true
METEOLORD_EXTERNAL_NETWORK=deny
METEOLORD_GRAFANA_INTERNAL_ENABLED=false

# Database — internal host only
DB_HOST=db
DB_PORT=5432
DB_NAME=meteolord_local
DB_USER=meteolord
DB_PASSWORD=local_synthetic_123

# Internal API key (synthetic, never productive)
INGEST_API_KEY=local-synthetic-key-2026

# Clau sintètica AES-256 per provar connectors locals. La primera entrada és la primària.
METEOLORD_CONNECTOR_KEYS=local-v1:c3ludGhldGljLWNvbm5lY3Rvci1rZXktMzJieXRlcyE
