:80 {
  encode gzip zstd

  # PYTHON API (manté el path complet)
  handle /api/v1/* {
    reverse_proxy api_py:8000
  }

  # NODE legacy (si cal)
  handle /api/* {
    reverse_proxy backend:3000
  }

  # STATIC
  root * /srv
  file_server
}
