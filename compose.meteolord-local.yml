name: meteolord-local

services:
  db:
    image: postgis/postgis@sha256:44126d872ac91993766c341e369c539e8196614321765d36a6f1bab0419a5fa5
    platform: linux/amd64
    environment:
      POSTGRES_USER: meteolord
      POSTGRES_PASSWORD: local_synthetic_123
      POSTGRES_DB: meteolord_local
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U meteolord"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - meteolord_core

  backend:
    # Immutable local MAP-A image; synthetic fixtures are bundled in the image.
    image: sha256:6eba5f1e1e8ccf7653aa3c21b614fa4dc55e1b2750d35ba1b0a1357c5fd34155
    pull_policy: never
    platform: linux/amd64
    env_file:
      - ./config/meteolord/local.env
    environment:
      PORT: "8080"
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
      POSTGRES_DB: meteolord_local
      POSTGRES_USER: meteolord
      POSTGRES_PASSWORD: local_synthetic_123
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./site:/site:ro
    ports:
      - "127.0.0.1:8088:8080"
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - >-
          fetch('http://localhost:8080/health')
          .then(response => process.exit(response.ok ? 0 : 1))
          .catch(() => process.exit(1))
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - meteolord_core

volumes:
  db_data:

networks:
  meteolord_core:
