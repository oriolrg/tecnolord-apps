// backend/tests/gpx.test.js
const { parsejarRuta } = require('../logic/gpx');

describe('Proves de lectura de tracks GPX', () => {
    test('Hauria de llegir correctament la distància i el nom', () => {
        // Aquí assumim que tens un fitxer de prova a la carpeta tests/fixtures
        // const ruta = parsejarRuta('./tests/fixtures/test.gpx');
        // expect(ruta.nom).toBe('Nom de la ruta');
        // expect(ruta.punts.length).toBeGreaterThan(0);
    });
});